version: '3.8'
services:
  fusion:
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - LOG_LEVEL=DEBUG
    command: python -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload