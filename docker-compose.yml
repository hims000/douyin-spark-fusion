version: '3.8'
services:
  fusion:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - ADMIN_PASSWORD=changeme
      - ALLOWED_ORIGIN=http://localhost:8000
    restart: unless-stopped